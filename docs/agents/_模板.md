# {Agent 名}设计文档

> 模板 v1(2026-08-25)。按 `docs/Agent开发流程_v1.0.md` 的九步组织,
> 每个 Agent 一份,随实际开发**持续回填**——设计文档跟代码脱节的那一刻
> 它就没用了。哪节写不出来,说明那一步还没想清,别跳过去。

## 元信息

- 状态:草稿 / 开发中 / 已上线 / 归档
- 需求来源:{PRD 路径或一句话意图}
- 设计稿:{artifact 画布链接 + 页名}
- 实例判断:四件套(落在 main-agent)/ 新实例(写明命中哪条判据:
  独立人设 / 独立权限集 / 独立上下文)

## S1 章法草稿(能力定义)——最先写

### 用户旅程章法

按步写:用户说什么 → 模型该干什么(动词)→ 屏上该有什么 → 嘴上说什么(附一个问题)。

### 能力清单

| 动词(草稿名) | 它做什么 | 返回里必须有什么 |
|---|---|---|

### 显示清单

| 屏显需求 | 落点(模板/阶段/通道) |
|---|---|

### 边界清单

- 灰(要确认):
- 黑(永不给):
- 明确不做:
- whenToUse 草稿(≤50 字,"做什么+何时用"双段式,宁过触发):

## S2 落点清单

| 需求 | 落点(数据/机制/策略/UI) | 备注 |
|---|---|---|

数据源选型(真实优先、零 Key 优先、国内不走隧道;mock 破例三条件):

| 数据 | 源 | 取舍与调研结论(写死,别日后重查) |
|---|---|---|

## S3 HMI 与对话脚本

- 设计稿:{链接};拍板记录:{日期,谁拍的}
- 对话脚本(五步表,与 S1 章法互为镜像)
- pilot 场景:{场景 id + 机器人性格指令}

## S4 域仓

- 数据形状(TS 接口贴这里)
- 持久化决策:什么进仓、什么明确不进(遥测/运行态)、淘汰策略
- localStorage 配额风险点

## S5 Tools

| 工具名 | 权限 | 一句话 | 返回里必须有 |
|---|---|---|---|

desc 写作自查:数组参数 items schema?答案在返回里?缺参一次报全?
状态提示走返回?

## S6 引擎(可选)

调度策略(数据字段化)、边沿/周期、与装配层的分工。

## S7 卡片

- 模板契约:fields / forms(各档 blocks,块只增不减)/ interactions
- 阶段与数据形状的对应(阶段=数据形状,不是状态机)
- 渲染要点与已知坑位对照(空白文本节点/flex-end/svg 锁高/滚动档位)

## S8 Skill 定稿

- 文件:agents/main-agent/skills/{xxx}.ts
- 官方对齐自查:清单开头?微例子?术语一致?whenToUse 双段式?
  pilot 场景 expectSkill 声明了?
- 草稿→定稿的差异(哪些动词改了名、哪些能力砍了)
- 上升到人设层的规则(章法里模型取了才看见的,哪些必须提前)

## S9 装配与验证

- 双装配点:director/main.ts {行为} · tests/pilot/run.ts {行为}
- NOT_WIRED 覆盖:{是/否}
- 验证现状:测试数 / 画廊肉眼 {日期} / pilot {场景+结论}
- 已知参数退化与宽容(观察轮记录)

## 变更记录

| 日期 | 变更 | 动因(实拍/决策) |
|---|---|---|
