# 复制为 .env.local（已 gitignore），按需填入自己的 Key。
# 所有 Key 都只在你的浏览器里使用，本项目零后端，不会上传到任何服务器。

# ── 必填：模型 ────────────────────────────────────────────
# OpenRouter（https://openrouter.ai/keys）
# 对话、快慢双层、子 Agent、联网搜索兜底、绘本插图（图像 API）都走它。
# 不填的话也可以在控制面板里直接输入。
VITE_OPENROUTER_KEY=

# ── 可选：导航 / 天气地名解析 / 地图 ─────────────────────
# 高德开放平台（https://console.amap.com），注意是两个不同类型的 Key：
# ① Web服务 Key —— navigation.* 搜索/算路/地理编码 用
VITE_AMAP_WEB_KEY=
# ② Web端(JS API) Key + 安全密钥 —— 车机屏加载活地图用。
#    控制台里要把 localhost 加进域名白名单。
VITE_AMAP_JS_KEY=
VITE_AMAP_JS_SECRET=

# 天气本身走 Open-Meteo，零 Key；但地名→坐标仍依赖上面的 Web服务 Key。

# ── 可选：内容源 ─────────────────────────────────────────
# NewsAPI（https://newsapi.org）
# ⚠ 免费层只对 localhost 开放且【禁止部署】——只用于本地开发。
VITE_NEWSAPI_KEY=

# Pexels（https://www.pexels.com/api/）短视频搜索
VITE_PEXELS_KEY=

# 音乐(iTunes)、电台(Radio Browser)、天气(Open-Meteo)零 Key，无需配置。

# ── 可选：云端 TTS（讯飞超拟人语音合成，https://www.xfyun.cn）──
# 配了之后控制面板音色下拉框里的「云端超拟人」组才真正生效；
# 不配就一直用浏览器本机音色。三个值都在讯飞控制台的应用页里。
VITE_XFYUN_APPID=
VITE_XFYUN_API_KEY=
VITE_XFYUN_API_SECRET=
